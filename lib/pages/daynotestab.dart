import 'package:flutter/material.dart';

class DayNotesTab extends StatelessWidget {
  const DayNotesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Day Notes Tab'),
    );
  }
}
